<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Primary Button Style -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
        <Setter Property="MinWidth" Value="120" />
        <Setter Property="MinHeight" Value="44" />
        <Setter Property="Padding" Value="24,8" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeBody}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusMedium}" />
    </Style>

    <!-- Secondary Button Style -->
    <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="MinWidth" Value="120" />
        <Setter Property="MinHeight" Value="44" />
        <Setter Property="Padding" Value="24,8" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeBody}" />
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusMedium}" />
    </Style>

    <!-- Subtle Button Style Override -->
    <Style x:Key="SubtleButtonStyleOverride" TargetType="Button" BasedOn="{StaticResource SubtleButtonStyle}">
        <Setter Property="MinWidth" Value="44" />
        <Setter Property="MinHeight" Value="44" />
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusSmall}" />
    </Style>

    <!-- Rounded TextBox Style -->
    <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
        <Setter Property="MinHeight" Value="44" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeBody}" />
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusMedium}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <!-- Search TextBox Style -->
    <Style x:Key="SearchTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource RoundedTextBoxStyle}">
        <Setter Property="PlaceholderText" Value="Search..." />
    </Style>

    <!-- Rounded ComboBox Style -->
    <Style x:Key="RoundedComboBoxStyle" TargetType="ComboBox">
        <Setter Property="MinHeight" Value="44" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeBody}" />
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusMedium}" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
    </Style>

    <!-- Progress Bar Style -->
    <Style x:Key="BrandedProgressBarStyle" TargetType="ProgressBar">
        <Setter Property="Height" Value="8" />
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusSmall}" />
        <Setter Property="Foreground" Value="{StaticResource BrandPrimaryBrush}" />
    </Style>

    <!-- NavigationView Style Customization -->
    <Style x:Key="BrandedNavigationViewStyle" TargetType="NavigationView">
        <Setter Property="PaneDisplayMode" Value="Left" />
        <Setter Property="IsBackButtonVisible" Value="Collapsed" />
        <Setter Property="IsPaneToggleButtonVisible" Value="True" />
        <Setter Property="OpenPaneLength" Value="280" />
        <Setter Property="CompactPaneLength" Value="48" />
    </Style>

    <!-- Card Button Style (for clickable cards) -->
    <Style x:Key="CardButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusMedium}" />
        <Setter Property="Padding" Value="{StaticResource SpacingMedium}" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="MinHeight" Value="80" />
    </Style>

    <!-- Info Bar Styles -->
    <Style x:Key="SuccessInfoBarStyle" TargetType="InfoBar">
        <Setter Property="Severity" Value="Success" />
        <Setter Property="IsClosable" Value="True" />
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusMedium}" />
    </Style>

    <Style x:Key="WarningInfoBarStyle" TargetType="InfoBar">
        <Setter Property="Severity" Value="Warning" />
        <Setter Property="IsClosable" Value="True" />
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusMedium}" />
    </Style>

    <Style x:Key="ErrorInfoBarStyle" TargetType="InfoBar">
        <Setter Property="Severity" Value="Error" />
        <Setter Property="IsClosable" Value="True" />
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusMedium}" />
    </Style>

    <!-- Toggle Switch Style -->
    <Style x:Key="SettingsToggleSwitchStyle" TargetType="ToggleSwitch">
        <Setter Property="MinWidth" Value="44" />
        <Setter Property="MinHeight" Value="44" />
    </Style>

    <!-- Radio Button Style -->
    <Style x:Key="SettingsRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="MinWidth" Value="44" />
        <Setter Property="MinHeight" Value="44" />
        <Setter Property="Padding" Value="12,8" />
    </Style>

</ResourceDictionary>
